pipeline {
    agent none
    options {
        parallelsAlwaysFailFast()
    }
    stages {
        stage('s0') {
            steps {
                echo "params: ${params.module} ${params.setupenv}"
            }
        }
    }
}
